
import 'package:chatapp/app.dart';
import 'package:flutter/material.dart';

void main() {
  runApp(
    // DevicePreview(
    //   enabled: !kReleaseMode,
    //   builder: (context) => const AppRoot(), // Wrap your app
    // ),
    const AppRoot(),
  );
}
